# PyTorch is installed separately so the CUDA build can be selected:
#   pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 \
#       --index-url https://download.pytorch.org/whl/cu118

transformers>=4.40
opencv-python>=4.8
numpy>=1.24,<2
Pillow>=10.0
tqdm>=4.65
